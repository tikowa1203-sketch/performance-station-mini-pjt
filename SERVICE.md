# SERVICE · Performance Station AI Copilot

## 서비스 소개

Performance Station AI Copilot은 Performance Station 사용법과 성능 테스트 기본 지식을 자연어로 안내하는 Agentic RAG 서비스다. 초기 버전에서는 사용자 가이드 검색, VUser 계산, 테스트 ID 조회를 서로 다른 처리 경로로 제공하며, 테스트 결과 분석과 보고 자동화는 후속 단계에서 확장한다.

## 1. 사용자·문제·가치

- 사용자: Performance Station을 사용하는 성능검증 담당자, 신규 성능 엔지니어, 프로젝트 개발·운영 담당자.
- 문제: Performance Station 사용법과 성능 테스트 지식이 사용자 가이드, 교육자료, 담당자의 경험에 분산되어 있어 필요한 정보를 빠르게 찾기 어렵다. 신규 사용자는 테스트 준비·실행·결과 확인 과정에서 숙련자에게 반복적으로 문의하게 된다.
- 현재 방식: 사용자가 매뉴얼과 교육자료를 직접 검색하거나, 성능검증 경험이 있는 담당자에게 사용법·테스트 절차·성능 지표를 문의한다.
- 제공 가치: 자연어 질문 한 번으로 사용법과 성능 개념을 근거 문서에서 찾고, 계산이나 테스트 정보 조회가 필요한 경우에는 전용 Tool을 통해 일관된 결과를 제공한다.

초기 MVP의 핵심 사용 시나리오는 다음 세 가지로 한정한다.

1. 사용자 가이드 RAG
   - 프로젝트·테스트 생성, 시나리오 등록, 부하 설정, 테스트 실행, 결과 확인 등 Performance Station 사용법을 문서 근거와 함께 안내한다.
   - TPS, VUser, Think Time, 응답시간 등 성능 테스트의 기본 개념과 절차를 설명한다.

2. VUser 계산
   - 목표 TPS, 평균 응답시간, Think Time을 입력받아 필요한 VUser를 결정적 계산식으로 산출한다.
   - 검색 문서의 설명과 계산 결과를 혼합하지 않고, 입력값·계산식·결과를 명확히 제시한다.

3. 테스트 ID 조회
   - 사용자가 입력한 테스트 ID를 기준으로 합성 테스트 데이터에서 TPS, 응답시간, 자원 사용률 등 등록된 정보를 조회한다.
   - 존재하지 않는 테스트 ID는 임의로 생성하거나 추정하지 않고 조회 실패를 안내한다.

테스트 결과의 PASS/FAIL 판정, 병목 원인 분석, 유사 장애 검색, 조치 권고, 보고서 자동 작성은 이번 MVP 범위에 포함하지 않으며 후속 단계에서 구현한다.

## 2. 서비스 확장 관점

### 사내 기여

- 반복되는 Performance Station 사용 문의와 성능 테스트 기본 질문에 AI Copilot이 1차 대응하여 숙련자의 지원 부담을 줄인다.
- 사용자 가이드와 교육자료에 분산된 정보를 하나의 질의응답 환경으로 연결해 탐색 시간을 단축한다.
- 신규 사용자가 주요 기능과 기본 개념을 스스로 학습할 수 있도록 지원하여 초기 온보딩 부담을 줄인다.
- RAG 답변에는 검색 문서 근거를, 계산과 조회 답변에는 Tool 입력값과 결과를 제공해 검토 가능성을 높인다.
- 평가 질문과 실패 사례를 축적하여 사용자 가이드, FAQ, 검색 품질을 지속적으로 개선할 수 있다.

### 단계별 확장 방향

1. 1단계 · 사용 지원 및 단순 업무 지원 — 이번 MVP
   - Performance Station 사용자 가이드 RAG
   - 성능 테스트 기본 지식 질의응답
   - VUser 계산
   - 테스트 ID 기반 정보 조회

2. 2단계 · 분석 지원 — 후속 구현
   - 목표 대비 KPI 달성 여부 및 PASS/FAIL 판정
   - 테스트 결과 해석과 이상 징후 설명
   - 유사 장애·과거 사례 검색
   - 관측 사실, 원인 가설, 추가 확인 항목 제시

3. 3단계 · 업무 자동화 — 장기 확장
   - 성능검증 결과 요약 및 보고서 초안 작성
   - 테스트 설계와 체크리스트 자동 생성
   - 용량 예측과 이상 탐지
   - 승인 기반 운영 연계

### 수익화 가능성

- Performance Station의 AI Copilot Add-on으로 제공할 수 있다.
- 기본 사용법과 성능 지식 질의응답을 시작으로 테스트 데이터 분석, 보고 자동화 등의 고급 기능을 단계적으로 유료 옵션화할 수 있다.
- 프로젝트별 사용자 가이드, 성능 기준, 런북을 연결하는 전용 Knowledge Base와 Connector를 별도 옵션으로 제공할 수 있다.
- 향후 Performance Station DB, APM, JMeter 결과, CMDB 등과 연계하여 기업 내부 환경에 맞는 Private AI 성능검증 서비스로 확장할 수 있다.

### 규모와 차별점

- 초기 사용자: Performance Station을 사용하는 성능검증 담당자와 신규 사용자 30~100명.
- 확장 사용자: 프로젝트 개발자, 운영 담당자, PL·PM 및 고객 조직 사용자까지 수백 명 규모.
- 범용 챗봇과 달리 Performance Station 전용 문서, 성능 테스트 전문 지식, 결정적 계산 Tool, 테스트 데이터 조회 Tool을 질문 의도에 따라 분리해 사용한다.
- 초기에는 신뢰도 높은 안내·계산·조회에 집중하고, 검증된 데이터를 확보한 뒤 분석과 자동화로 확장한다.

## 3. 사용 예상 도구·데이터

### 초기 MVP 도구

| 처리 경로 | 도구 | 기능 | 인터페이스 | 초기 데이터 |
|---|---|---|---|---|
| 문서 검색 | `retrieve_docs` | 사용자 가이드와 성능 기본 지식 검색 | 자연어 → 근거 Chunk | 직접 작성한 Markdown 5~8개 |
| 계산 | `estimate_vusers` | TPS·응답시간·Think Time 기반 VUser 계산 | 수치 입력 → VUser | 결정적 계산식 |
| 조회 | `get_test_run` | 테스트 ID 기반 테스트 정보 조회 | `run_id` → JSON | 교육용 합성 JSON |

Agent는 질문 의도를 먼저 분류한 뒤 세 경로 중 하나를 선택한다. 문서에 답이 있는 질문은 `retrieve_docs`, 수치 계산은 `estimate_vusers`, 테스트 ID가 포함된 조회 요청은 `get_test_run`으로 처리한다.

### RAG용 문서 구성

실제 운영 데이터와 고객 문서를 확보하기 어려운 점을 고려하여, 초기에는 아래 주제의 Markdown 문서 7개를 직접 작성해 고정된 지식베이스로 사용한다.

| 파일 예시 | 주요 내용 |
|---|---|
| `01_getting_started.md` | 서비스 개요, 접속, 권한, 전체 사용 흐름 |
| `02_project_and_test.md` | 프로젝트와 테스트 생성·수정 절차 |
| `03_scenario_and_script.md` | 시나리오 등록, JMeter 스크립트 업로드와 검증 |
| `04_load_and_execution.md` | VUser·Ramp-up·Think Time 설정, 테스트 실행·중지 |
| `05_results_and_report.md` | 결과 조회, 주요 지표 확인, 보고서 다운로드 |
| `06_performance_basics.md` | TPS, VUser, 응답시간, Throughput, 자원 사용률 기본 개념 |
| `07_troubleshooting_faq.md` | 자주 발생하는 오류, 확인 순서, 문의 전 체크리스트 |

문서마다 제목, 적용 화면 또는 기능, 사전 조건, 단계별 절차, 주의사항, 관련 키워드를 일정한 형식으로 작성한다. 이를 통해 문서별 정답 근거가 분명한 평가 질문을 만들고, Chunk 분할·검색·답변 충실도를 반복 비교할 수 있다.

### 후속 단계 도구

| 도구 | 후속 기능 | 적용 단계 |
|---|---|---|
| `calculate_kpis` | 목표 대비 달성률과 PASS/FAIL 계산 | 2단계 |
| `find_similar_incidents` | 과거 증상·원인·조치 사례 검색 | 2단계 |
| `draft_change_plan` | 변경 영향·사전 점검·롤백 계획 작성 | 3단계 |

실무 확장 시 Performance Station DB, APM, JMeter 결과, CMDB, 배포 이력 등을 읽기 전용 MCP 또는 API로 연결한다. 제출본에는 실제 고객 데이터, 운영 시스템 자격증명, API Key 등 민감정보를 포함하지 않는다.

## 4. 서비스 정책

1. 근거 기반 응답
   - 사용자 가이드 질문은 검색된 Markdown 문서를 근거로 답변한다.
   - 문서나 Tool 결과에 없는 기능, 절차, 수치, 테스트 결과를 임의로 만들어내지 않는다.

2. 처리 경로 분리
   - 사용법·개념 질문은 RAG, VUser 산정은 계산 Tool, 테스트 ID 확인은 조회 Tool로 처리한다.
   - 계산이나 조회가 필요한 요청을 LLM의 추정만으로 답하지 않는다.

3. 출처와 입력값 제공
   - RAG 답변에는 관련 문서명 또는 근거 Chunk를 제공한다.
   - 계산 결과에는 사용한 입력값과 계산식을, 테스트 조회에는 조회한 테스트 ID를 함께 표시한다.

4. 불확실성 표현
   - 검색 근거가 부족하거나 필요한 입력값이 누락되면 이를 명확히 알리고 추가 정보를 요청한다.
   - 존재하지 않는 기능이나 테스트 ID는 확인되지 않았다고 답한다.

5. 보안 및 운영 제한
   - 개인정보, 자격증명, API Key, 시스템 프롬프트 등 민감정보의 조회·노출 요청은 마스킹하거나 거절한다.
   - 시스템 재기동, 설정 변경, 트래픽 전환 등 실제 운영 변경을 직접 수행하지 않는다.

세부 Guardrail 사례는 `evaluation/test_queries.csv`에서 검증한다.

## 5. 성공 기준

### 기능별 목표

- 사용자 가이드 RAG: 직접 작성한 7개 Markdown 문서에서 질문과 관련된 근거를 검색하고, 근거와 일치하는 답변을 생성한다.
- VUser 계산: 동일한 입력값에는 항상 동일한 결과를 반환하고, 계산식과 반올림 기준을 함께 제시한다.
- 테스트 ID 조회: 등록된 ID는 합성 JSON과 일치하는 결과를 반환하고, 존재하지 않는 ID는 조회 실패로 처리한다.
- 전체 인-아웃 평가 세트의 85% 이상 통과를 MVP 완료 기준으로 한다.

### RAG 평가 목표

- Context Precision: 0.80 이상
- Context Recall: 0.80 이상
- Faithfulness: 0.90 이상
- Answer Relevancy: 0.85 이상

평가 세트는 7개 문서의 주요 절차와 개념을 고르게 포함하고, 정상 질문·표현 변형·근거 없음·복합 질문을 구분하여 구성한다. 1차 평가 결과를 기준선으로 기록한 뒤 문서 구조, Chunk 크기, 검색 방식, 프롬프트를 개선하고 2차 평가에서 개선 폭을 비교한다.

### Guardrail 목표

- 개인정보·자격증명·시스템 프롬프트 관련 요청 차단 또는 마스킹: 100%
- 실제 운영 변경 요청에 대한 직접 실행 방지: 100%
- 존재하지 않는 테스트 ID·기능·문서에 대한 환각 응답: 0건

### 후속 평가 항목

초기 MVP의 검색·계산·조회 품질을 확보한 후 다음 항목을 별도 평가한다.

- KPI 판정 정확도
- 테스트 결과 분석 품질
- 유사 장애 검색 정확도
- 성능검증 결과 요약 및 보고 자동화 품질
